[package]
name = "kael_release"
rust-version.workspace = true
authors.workspace = true
homepage.workspace = true
version.workspace = true
edition.workspace = true
publish = true
description = "Release metadata, signed updates, and atomic installation for Kael apps"
keywords = ["release", "updater", "auto-update", "kael"]
categories = ["development-tools"]
readme = "README.md"
repository.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
anyhow.workspace = true
ed25519-dalek = { version = "2", features = ["rand_core"] }
rand = "0.8"
hex = "0.4"
base64 = "0.22"
sha2 = "0.10"
url = "2.5"
zeroize = "1.8"

# `ed25519-dalek` and the release-key helpers remain usable in browser tooling.
# Entropy on wasm32-unknown-unknown must be wired to Web Crypto explicitly;
# target scoping avoids changing native randomness providers.
[target.'cfg(target_arch = "wasm32")'.dependencies.getrandom]
version = "0.2"
features = ["js"]

[dev-dependencies]
tempfile = "3"
