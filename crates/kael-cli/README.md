# kael-cli

The project scaffolder for [Kael](https://github.com/Augani/kael).

```bash
cargo install kael-cli
kael new my_app
cd my_app
cargo run
```

The generated project uses the matching `kael` and `kael_ui` release and can
be created offline after the CLI is installed.

Licensed under Apache-2.0.
